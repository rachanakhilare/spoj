#include <iostream>

using namespace std;

int main()
{

    int n,i,sum,t;
    cin>>t;
    while(t--)
    {
    cin>>n;
    int a[n];
    sum=(n*(n+1))/2;
    for(i=0;i<n-1;i++)
    {
        cin>>a[i];
        sum-=a[i];
    }
    cout<<sum<<"\n";
    }
    return 0;
}
